# 평가 준비

P2에서 고정 RNG fixture, P3에서 mock GM 응답, P4에서 실제 모델 평가를 추가한다.
시나리오는 `scenarios/`, 입력 상태는 `fixtures/`, 결과는 `reports/`로 분리한다.
현재 실행 가능한 AI 평가나 평가 통과 결과는 없다.
목표 시나리오와 통과 기준은 `docs/implementation-plan.md`를 따른다.
